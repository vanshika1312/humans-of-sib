import { RouteLoadingFallback } from "@/components/ui/route-loading-fallback";

export default function HiringActivityLoading() {
  return <RouteLoadingFallback label="Loading activity history…" />;
}
