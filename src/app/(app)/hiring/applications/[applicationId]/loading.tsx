import { RouteLoadingFallback } from "@/components/ui/route-loading-fallback";

export default function HiringApplicationDetailLoading() {
  return <RouteLoadingFallback label="Loading application…" />;
}
